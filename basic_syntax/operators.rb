# OPERADORES:

# ARITMÉTICOS:
# + (Adição);
# - (Subtração);
# * (Multiplicação);
# / (Divisão),
# % (Resto da divisão);
# ** (Exponenciação).

# COMPARAÇÃO:
# == (igual á);
# != (Diferente de);
# > (Maior que);
# < (Menor que);
# >= (Maior ou gual á);
# <= (Menro ou igual á).

# LÓGICOS:
# && (e);
# || (ou);
# ! (negação).

# ATRIBUIÇÃO: (=).


# EXEMPLOS: 

# a = 10
# b = 5
# soma = a + b
# subtracao = a - b
# multiplicacao = a * b
# divisao = a / b
# resto_da_divisao = a % b
# e_igual = (a == b)
